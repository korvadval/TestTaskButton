import { ViewerDisabledDirective } from "./viewer-disabled.directive";

describe('ViewerDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewerDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
